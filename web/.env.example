# Where the API lives, including its /api/v1 prefix.
# CORS_ORIGINS on the server must list this app's origin, and it already
# defaults to http://localhost:5173 (Vite) and http://localhost:3000.
VITE_API_URL=http://localhost:4000/api/v1
